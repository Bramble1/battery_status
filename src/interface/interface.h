#include "core/core.h"

/*Interface.h relies upon and uses the functions in core folder
 * Interface.* is used as the name implies and to make main look cleaner,everything done in here
 * would be written in main()*/

void process_user_input (int *argc,char *argv[],double *minutes);
void init_battery_monitor(double *minutes);
